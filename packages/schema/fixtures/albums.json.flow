// @flow
import type { UserStats } from './stats';

export type AlbumSource = {
  id: number,
  name: string,
  artworkUrl?: string,
  artistId: number,
  songIds: number[],
  releaseYear: number,
  timeAdded?: number,
  stats?: UserStats,
}

declare export var albums: AlbumSource[];
