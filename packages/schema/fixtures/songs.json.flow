// @flow
import type { SongUserStats } from './stats';

export type SongSource = {
  id: number,
  streamUrl: string,
  name: string,
  duration: number,
  trackNumber?: number,
  diskNumber?: number,
  timeAdded?: number,

  artistIds?: number[],
  albumId: number,
  stats?: SongUserStats,
}

declare export var songs: SongSource[];
