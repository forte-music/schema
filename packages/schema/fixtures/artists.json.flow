// @flow
import type { UserStats } from './stats';

export type ArtistSource = {
  id: string,
  name: string,
  timeAdded?: number,
  albumIds: string[],
  stats?: UserStats,
}

declare export var artists: ArtistSource[];
