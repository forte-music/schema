{
  "name": "@forte-music/schema",
  "publishConfig": {
    "access": "public"
  },
  "version": "0.0.42",
  "description": "The GraphQL schema for communication with forte.",
  "files": [
    "schema.graphql",
    "schema.json",
    "schema.json.d.ts",
    "fixtures/"
  ],
  "repository": "git@github.com:forte-music/schema.git",
  "author": "Martin Charles <martincharles07@gmail.com>",
  "license": "MIT",
  "scripts": {
    "prepack": "yarn build",
    "lint": "graphql lint",
    "build": "yarn generate-schema && yarn compile-fixtures",
    "compile-fixtures": "node build.js",
    "generate-schema": "apollo schema:download schema.json --endpoint schema.graphql"
  },
  "devDependencies": {
    "apollo": "^1.6.0",
    "graphql-cli": "^2.16.3",
    "toml": "^2.3.3"
  },
  "dependencies": {
    "@types/graphql": "^0.13.2"
  },
  "graphql-schema-linter": {
    "rules": [
      "defined-types-are-used",
      "enum-values-all-caps",
      "types-are-capitalized"
    ]
  }
}
