// @flow
import type { UserStats } from './stats';

export type AlbumSource = {
  id: string,
  name: string,
  artworkUrl: string,
  artistId: string,
  songIds: string[],
  releaseYear: number,
  stats?: UserStats,
}

declare export var albums: AlbumSource[];
