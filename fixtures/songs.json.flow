// @flow
import type { SongUserStats } from './stats';

export type SongSource = {
  id: string,
  streamUrl: string,
  name: string,
  duration: number,
  trackNumber?: number,
  diskNumber?: number,

  artistIds?: string[],
  albumId: string,
  stats?: SongUserStats,
}

declare export var songs: SongSource[];
